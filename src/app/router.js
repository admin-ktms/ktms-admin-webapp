import{renderAdminShell}from'../layout/AdminShell.js';const routes=new Map([['/',{title:'Dashboard',description:'KTMS operational overview.'}],['/tournaments',{title:'Tournaments',description:'Tournament management will be implemented in the tournament operations phase.'}],['/registrations',{title:'Registrations',description:'Registration management will be implemented in the tournament operations phase.'}],['/players',{title:'Players',description:'Player management will be implemented in the tournament operations phase.'}],['/payments',{title:'Payments',description:'Payment operations will be implemented in the platform operations phase.'}],['/matchdays',{title:'Matchdays',description:'Matchday management will be implemented in the tournament operations phase.'}],['/fixtures',{title:'Fixtures',description:'Fixture management will be implemented in the tournament operations phase.'}],['/results',{title:'Results',description:'Result management will be implemented in the tournament operations phase.'}],['/standings',{title:'Standings',description:'Standings will be implemented in the tournament operations phase.'}],['/progression',{title:'Progression',description:'Progression will be implemented in the tournament operations phase.'}],['/awards',{title:'Awards',description:'Awards will be implemented in the tournament operations phase.'}],['/notifications',{title:'Notifications',description:'Notifications will be implemented in the platform operations phase.'}],['/support',{title:'Support',description:'Support operations will be implemented in the platform operations phase.'}],['/operations',{title:'Operations',description:'Platform operations will be implemented in the platform operations phase.'}],['/audit',{title:'Audit',description:'Audit operations will be implemented in the platform operations phase.'}],['/administrators',{title:'Administrators',description:'Administrator management will be implemented in the platform operations phase.'}]]);export function resolvePath(){const hash=window.location.hash.replace(/^#/,'');return hash||'/'}export function renderRoute(){const path=resolvePath();const route=routes.get(path)||{title:'Page not found',description:'The requested Admin Web App route does not exist.'};document.querySelector('#app').innerHTML=renderAdminShell({path,content:`<section><p style="color:var(--ktms-secondary);margin:0 0 8px">KTMS ADMIN</p><h1 style="margin:0 0 10px">${route.title}</h1><p style="color:var(--ktms-secondary);max-width:720px">${route.description}</p></section>`})}export function startRouter(){window.addEventListener('hashchange',renderRoute);renderRoute()}
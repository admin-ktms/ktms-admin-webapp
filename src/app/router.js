const routes=new Map();export function registerRoute(path,render){routes.set(path,render);}export function resolveRoute(pathname=window.location.pathname){return routes.get(pathname)||routes.get('*');}export function navigate(path){if(window.location.pathname!==path)window.history.pushState({},'',path);window.dispatchEvent(new PopStateEvent('popstate'));}export function startRouter(){const render=()=>{const handler=resolveRoute();if(handler)handler();};window.addEventListener('popstate',render);window.addEventListener('click',e=>{const link=e.target.closest('[data-route]');if(!link||e.defaultPrevented)return;e.preventDefault();navigate(link.getAttribute('href'));});render();}